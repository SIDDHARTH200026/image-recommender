# GUVI Image Recommendation System ✅

## Quick Start
1. `pip install -r requirements.txt`
2. `python main.py`  ← **THIS CREATES EVERYTHING**
3. Open `results.png`

## Features
- ✅ ResNet50 + VGG16 pretrained
- ✅ Feature extraction saved to disk  
- ✅ CrossEntropyLoss + Adam optimizer
- ✅ Similarity recommendations
- ✅ Web prototype ready

VS Code Setup (3 Minutes):
Create folder image_recommender
Copy ALL 10 files above into it
Open in VS Code: File → Open Folder
Terminal (Ctrl+`):
pip install -r requirements.txt
python main.py

That's it! Results in results.png + web app ready.
